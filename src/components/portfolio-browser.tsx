"use client";
import { useState } from "react"; import { categories, projects } from "@/content/portfolio"; import { ProjectGrid } from "./project-grid";
export function PortfolioBrowser(){const [active,setActive]=useState<(typeof categories)[number]>("All work");const items=active==="All work"?projects:projects.filter(p=>p.categories.includes(active));return <div className="portfolio-browser"><div className="filters" role="group" aria-label="Filter portfolio projects">{categories.map(c=><button key={c} onClick={()=>setActive(c)} aria-pressed={active===c}>{c}</button>)}</div>{items.length?<ProjectGrid items={items}/>:<p className="empty">Work in this category is being prepared for publication. Ask us about relevant examples.</p>}<style>{`.filters{display:flex;flex-wrap:wrap;gap:.5rem;margin-bottom:4rem}.filters button{padding:.65rem .95rem;border:1px solid var(--color-line);background:transparent;border-radius:var(--radius-full);cursor:pointer;font-size:.83rem}.filters button[aria-pressed=true]{background:var(--forest-950);color:var(--cream-50);border-color:var(--forest-950)}.empty{padding:4rem;border:1px solid var(--color-line);text-align:center}`}</style></div>}
