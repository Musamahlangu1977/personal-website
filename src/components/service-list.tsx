import Link from "next/link"; import { ArrowUpRight } from "lucide-react"; import type { Service } from "@/content/services"; import { actions } from "@/content/site";
export function ServiceList({ items, numbered=true }: { items: Service[]; numbered?: boolean }) { return <div className="service-list">{items.map((service,i)=><article key={service.id} className="service-row" data-panel><span className="service-number">{numbered?String(i+1).padStart(2,"0"):service.group.split(" ")[0]}</span><h3>{service.title}</h3><div><p>{service.description}</p><strong>{service.outcome}</strong><Link href={`/services#${service.id}`}>{actions.learn}<ArrowUpRight size={16}/></Link></div></article>)}<style>{`.service-list{border-top:1px solid var(--color-line)}.service-row{display:grid;grid-template-columns:5rem minmax(16rem,.85fr) minmax(18rem,1fr);gap:2rem;padding:2.4rem 0;border-bottom:1px solid var(--color-line)}.service-number{color:var(--gold-600);font-weight:700;font-size:.78rem;letter-spacing:.12em;text-transform:uppercase}.service-row h3{margin:0;font:600 clamp(1.45rem,2.5vw,2.25rem)/1.12 var(--font-display);letter-spacing:-.035em}.service-row p{margin:0;color:var(--color-muted)}.service-row strong{display:block;margin:1rem 0;font-size:.9rem}.service-row a{display:inline-flex;align-items:center;gap:.5rem;font-weight:700;border-bottom:1px solid currentColor}.dark .service-list,.dark .service-row{border-color:rgb(250 248 242/.18)}.dark .service-row p{color:rgb(250 248 242/.65)}@media(max-width:760px){.service-row{grid-template-columns:3rem 1fr}.service-row>div{grid-column:2}.service-row h3{min-width:0}}`}</style></div> }
