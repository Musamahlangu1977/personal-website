export function SectionHeading({ eyebrow, title, intro, light=false }: { eyebrow: string; title: string; intro?: string; light?: boolean }) { return <div className={`section-heading ${light ? "light" : ""}`}><span className="eyebrow">{eyebrow}</span><div><h2 className="section-title">{title}</h2>{intro&&<p className="lede">{intro}</p>}</div><style>{`.section-heading>div{display:grid;grid-template-columns:minmax(0,1.45fr) minmax(18rem,.75fr);gap:3rem;align-items:end}.section-heading .lede{margin:0 0 .35rem}.section-heading.light .eyebrow{color:var(--gold-400)}@media(max-width:760px){.section-heading>div{grid-template-columns:1fr;gap:1.5rem}.section-heading .lede{margin:0}}`}</style></div> }
