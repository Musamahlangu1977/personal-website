import { Suspense } from "react";import { EnquiryForm } from "@/components/enquiry-form";import { PageHero } from "@/components/page-hero";import { contact } from "@/content/contact";import { site,whatsappUrl } from "@/content/site";import { pageMetadata,seo } from "@/content/seo";
export const metadata=pageMetadata(seo.contact.title,seo.contact.description,"/contact");
export default function Contact(){return <main id="main-content"><PageHero eyebrow="Start your project" title={contact.heading} intro={contact.intro}/><section className="section contact-page"><div className="container"><aside><span className="eyebrow">Direct contact</span><h2>{contact.consultation}</h2><a href={`mailto:${site.email}`}>{site.email}</a><a href={whatsappUrl()} target="_blank" rel="noreferrer">{site.phone}</a><p>{site.location}</p></aside><Suspense fallback={<div>Loading enquiry form…</div>}><EnquiryForm/></Suspense></div></section><style>{`.contact-page>.container{display:grid;grid-template-columns:.55fr 1fr;gap:clamp(2rem,7vw,7rem)}.contact-page aside{position:sticky;top:7rem;align-self:start}.contact-page aside h2{font:600 clamp(2rem,4vw,3.5rem)/1.05 var(--font-display);letter-spacing:-.04em}.contact-page aside a{display:block;margin:.8rem 0;font-weight:700}.contact-page aside p{margin-top:2rem;color:var(--color-muted)}@media(max-width:800px){.contact-page>.container{grid-template-columns:1fr}.contact-page aside{position:static}}`}</style></main>}
