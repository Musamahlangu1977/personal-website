import Link from "next/link";import { ArrowUpRight } from "lucide-react";import { PageHero } from "@/components/page-hero";import { articles,resourcesCopy } from "@/content/resources";import { pageMetadata,seo } from "@/content/seo";
export const metadata=pageMetadata(seo.resources.title,seo.resources.description,"/resources");
export default function Resources(){return <main id="main-content"><PageHero eyebrow="Resources & insights" title={resourcesCopy.heading} intro={resourcesCopy.intro}/><section className="section resource-list"><div className="container">{articles.map((a,i)=><Link key={a.slug} href={`/resources/${a.slug}`}><span>{String(i+1).padStart(2,"0")}</span><div><small>{a.category} · {a.minutes}</small><h2>{a.title}</h2><p>{a.description}</p></div><ArrowUpRight/></Link>)}</div></section><style>{`.resource-list a{display:grid;grid-template-columns:4rem 1fr auto;gap:1rem;padding:2rem 0;border-top:1px solid var(--color-line)}.resource-list a:last-child{border-bottom:1px solid var(--color-line)}.resource-list>a span,.resource-list small{color:var(--gold-600);font-size:.72rem;text-transform:uppercase;letter-spacing:.1em}.resource-list h2{margin:.7rem 0;font:600 clamp(1.6rem,3vw,3rem)/1.1 var(--font-display);letter-spacing:-.04em}.resource-list p{max-width:45rem;margin:0;color:var(--color-muted)}@media(max-width:600px){.resource-list a{grid-template-columns:2.5rem 1fr}.resource-list svg{display:none}}`}</style></main>}
