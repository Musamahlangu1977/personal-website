import Link from "next/link";import { notFound } from "next/navigation";import { ArrowLeft } from "lucide-react";import { articles,resourcesCopy } from "@/content/resources";import { siteUrl } from "@/content/seo";
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)return{};return{title:a.title,description:a.description,alternates:{canonical:`/resources/${slug}`},openGraph:{title:a.title,description:a.description,url:`${siteUrl}/resources/${slug}`,type:"article"}}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();return <main id="main-content"><article className="article"><header className="dark"><div className="container"><Link href="/resources"><ArrowLeft size={16}/>{resourcesCopy.back}</Link><span>{a.category} · {a.minutes}</span><h1>{a.title}</h1><p>{a.description}</p></div></header><div className="article-body container"><aside>{resourcesCopy.byline}<br/>General information, not recruitment or legal advice.</aside><div>{a.sections.map(s=><section key={s.title}><h2>{s.title}</h2><p>{s.text}</p>{s.points&&<ul>{s.points.map(p=><li key={p}>{p}</li>)}</ul>}</section>)}</div></div></article><style>{`.article header{position:relative;overflow:hidden;padding:7rem 0 8rem;background:radial-gradient(circle at 78% 38%,rgb(255 255 255/.1),transparent 20rem),linear-gradient(rgb(255 255 255/.04) 1px,transparent 1px),linear-gradient(90deg,rgb(255 255 255/.04) 1px,transparent 1px),#000;background-size:auto,5rem 5rem,5rem 5rem}.article header a{display:inline-flex;align-items:center;gap:.5rem}.article header span{display:block;margin-top:6rem;color:var(--gold-400);font-size:.72rem;text-transform:uppercase;letter-spacing:.12em}.article h1{max-width:14ch;margin:1rem 0;font:600 clamp(3.2rem,7vw,7rem)/.95 var(--font-display);letter-spacing:-.065em}.article header p{max-width:40rem;color:rgb(250 248 242/.68);font-size:1.2rem}.article-body{display:grid;grid-template-columns:.4fr 1fr;gap:6rem;padding-block:var(--section-space)}.article-body aside{position:sticky;top:7rem;align-self:start;color:var(--color-muted);font-size:.8rem}.article-body section{margin-bottom:4rem}.article-body h2{font:600 clamp(2rem,4vw,3.5rem)/1.05 var(--font-display);letter-spacing:-.04em}.article-body p,.article-body li{font-size:1.1rem;color:var(--color-muted)}@media(max-width:700px){.article-body{grid-template-columns:1fr;gap:3rem}.article-body aside{position:static}}`}</style></main>}

