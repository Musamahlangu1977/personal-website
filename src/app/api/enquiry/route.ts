import { NextResponse } from "next/server";import { validateEnquiry } from "@/lib/enquiry";
export async function POST(request:Request){let data:unknown;try{if(Number(request.headers.get("content-length")||0)>15000)return NextResponse.json({ok:false,error:"Request too large"},{status:413});data=await request.json()}catch{return NextResponse.json({ok:false,error:"Invalid request"},{status:400})}const errors=validateEnquiry(data);if(errors.length)return NextResponse.json({ok:false,errors},{status:400});const url=process.env.ENQUIRY_WEBHOOK_URL;if(!url)return NextResponse.json({ok:false,error:"Online enquiries are not configured"},{status:503});try{const response=await fetch(url,{method:"POST",headers:{"content-type":"application/json",...(process.env.ENQUIRY_WEBHOOK_TOKEN?{authorization:`Bearer ${process.env.ENQUIRY_WEBHOOK_TOKEN}`}:{})},body:JSON.stringify({...(data as object),source:"mahlangu-online-solutions-website",receivedAt:new Date().toISOString()}),signal:AbortSignal.timeout(8000)});if(!response.ok)throw new Error("Delivery rejected");return NextResponse.json({ok:true})}catch{return NextResponse.json({ok:false,error:"Delivery failed"},{status:502})}}
