import type { MetadataRoute } from "next";import { articles } from "@/content/resources";import { projects } from "@/content/portfolio";import { siteUrl } from "@/content/seo";
export default function sitemap():MetadataRoute.Sitemap{const paths=["","/services","/work","/about","/process","/pricing","/resources","/contact","/privacy","/terms",...projects.map(p=>`/work/${p.slug}`),...articles.map(a=>`/resources/${a.slug}`)];return paths.map((p,i)=>({url:`${siteUrl}${p}`,lastModified:new Date(),changeFrequency:i===0?"weekly":"monthly",priority:i===0?1:p.includes("/work/")||p.includes("/resources/")?.7:.8}))}
