import { PageHero } from "@/components/page-hero"; import { FinalCta } from "@/components/final-cta"; import { process } from "@/content/site"; import { pageMetadata,seo } from "@/content/seo";
export const metadata=pageMetadata(seo.process.title,seo.process.description,"/process");
export default function Process(){return <main id="main-content"><PageHero eyebrow="How we work" title="A clear path.\nFrom first message to final file." intro={process.intro}/><section className="section process-page"><ol className="container">{process.steps.map((s,i)=><li key={s.title}><span>{String(i+1).padStart(2,"0")}</span><div><h2>{s.title}</h2><p>{s.body}</p></div></li>)}</ol></section><section className="section-compact process-note"><div className="container"><span className="eyebrow">What to expect</span><h2>Clear scope. Thoughtful questions. No disappearing act.</h2><p>We agree on the project, fee and expected turnaround before work begins. Reviews happen collaboratively, and final files are handed over in the formats agreed for your project.</p></div></section><FinalCta/><style>{`.process-page ol{list-style:none;padding:0}.process-page li{display:grid;grid-template-columns:12vw 1fr;padding:clamp(3rem,7vw,6rem) 0;border-bottom:1px solid var(--color-line)}.process-page li>span{color:var(--gold-600);font:500 clamp(2rem,5vw,5rem)/1 var(--font-display)}.process-page li>div{display:grid;grid-template-columns:1fr 1fr;gap:3rem}.process-page h2{margin:0;font:600 clamp(2rem,4vw,4rem)/1 var(--font-display);letter-spacing:-.05em}.process-page p{margin:0;color:var(--color-muted);font-size:1.1rem}.process-note{background:var(--cream-100)}.process-note h2{max-width:15ch;font:600 clamp(2.6rem,5vw,5rem)/1 var(--font-display);letter-spacing:-.05em}.process-note p{max-width:40rem;margin-left:auto;color:var(--color-muted);font-size:1.1rem}@media(max-width:700px){.process-page li{grid-template-columns:3.5rem 1fr}.process-page li>div{grid-template-columns:1fr}}`}</style></main>}
