# Accessibility

Maintain landmarks, one page heading, logical heading levels, visible keyboard focus, labels, sufficient contrast and the skip link. Prefer native HTML. Icon-only controls need names. Test keyboard navigation, 200% zoom, reduced motion and 320, 375 and 430 px layouts. Images require useful alt text; announced errors cannot rely on colour alone.
