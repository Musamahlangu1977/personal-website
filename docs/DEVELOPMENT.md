# Development

Use `README.md` commands. Next.js App Router, strict TypeScript and Tailwind CSS are established. Use two-space indentation. Content belongs in `src/content`, logic in `src/lib` and reusable UI in `src/components`. Run lint, types, tests and production build after material changes. Test behaviour and regressions rather than static snapshots.
